{
  "name": "@repo/echo-service",
  "version": "1.0.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist/* .turbo/* .eslintcache *.tsbuildinfo",
    "test": "vitest run",
    "test:w": "vitest watch",
    "build": "vite build",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/eslint-config": "^0.0.0",
    "@repo/typescript-config": "^0.0.0",
    "@types/aws-lambda": "^8.10.150",
    "aws-lambda": "^1.0.7",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@repo/utils": "^0.0.0"
  }
}
