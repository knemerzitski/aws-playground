{
  "name": "echo",
  "version": "1.0.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist/*",
    "build": "vite-node scripts/build.ts src/handler.ts --outfile dist/index.mjs"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.150",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.25.6"
  }
}
